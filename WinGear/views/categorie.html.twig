{% extends "layout.html.twig" %}

{% block title %}{{ categorie.name }}{% endblock %}

{% block content %}
    
    <h2 class="text-center">{{ block('title') }}</h2>
 <article >
     
    <div id="content"> 
        {% if categorie %}
        <form class="form-horizontal" role="form">          
        <div class="well">
        
        <div class="row">
            {% for article in articles %} 
            <div class="col-md-3 col-sm-6 col-xs-8 dl-horizontal">
                
                
                
                    <div class="thumbnail"> 
                        
                        <img src="../img/{{ article.image }}" alt="..."> 
                        <div class="caption">
                        <p>{{ article.title }} <p>
                        <p>{{ article.price }} € </p>             
                        <p><a href="/article/{{ article.id }}" class="btn btn-primary" role="button">Voir</a></p>  
                       </div> 
           
                </div>
                    </div>
            {% endfor %}
        
        </div>
        

    </div>
    </form>
    </div>
                                        
                                      
                                       
                                        
                       
</article>
            {% else %}
                <div class="alert alert-warning">Aucune catégorie trouvée.</div>
            {% endif %}   



        </form>
    </div>
{% endblock %}