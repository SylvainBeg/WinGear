{% extends 'layout.html.twig' %}
{% set adminMenu = true %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
<h2 id="Text_Admin_Article" class="text-center">{{ block('title') }}</h2>
{% for flashMessage in app.session.flashbag.get('success') %}
<div id="Text_Admin_Article" class="alert alert-success">
    {{ flashMessage }}
</div>
{% endfor %}

<div class="well">
{{ form_start(commentForm, { 'attr': {'class': 'form-horizontal'} }) }}
    <div id="Text_Admin_Article" class="form-group">
        {{ form_label(commentForm.content, null, { 'label_attr':  {
            'class': 'col-sm-4 control-label'
        }}) }}
        <div id="Text_Admin_Article" class="col-sm-6">
            {{ form_errors(commentForm.content) }}
            {{ form_widget(commentForm.content, { 'attr':  {
                'class': 'form-control',
                'rows': '4'                
            }}) }}
        </div>
    </div>
    <div id="Text_Admin_Article" class="form-group">
        <div id="Text_Admin_Article" class="col-sm-offset-4 col-sm-3">
            <input type="submit" class="btn btn-primary" value="Save" />
        </div>
    </div>
{{ form_end(commentForm) }}
</div>
{% endblock %}